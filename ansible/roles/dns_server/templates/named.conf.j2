options {
    listen-on port 53 { any; };
    directory       "/var/named";
    allow-query     { any; };
    recursion yes;
    forwarders { 8.8.8.8; };
};

zone "sesac.cloud.com" IN {
    type master;
    file "zone.sesac.cloud.com";
};
